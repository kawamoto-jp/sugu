<div class="registration">
  <div class="welcome">
    <p class="big_welcome">ようこそ, S U G Uへ</p>
    <p class="small_welcome">S U G Uは、「今すぐ会いたい」を形にする<br>「複数人」対「複数人」のマッチングアプリです。</p>
  </div>
  <div class="registration_user">
    <h2 class="devise_h2">新規登録</h2>

    <%= form_with model: @user, url: user_registration_path, class: 'registration-main', local: true do |f| %>
      <%= render "devise/shared/error_messages", resource: resource%>

      <div class="field">
        <div class="item"><%= f.label "ニックネーム" %></div>
        <%= f.text_field :nickname, autofocus: true, autocomplete: "nickname", class:"text" %>
      </div>

      <div class="field">
        <div class="item"><%= f.label "性別" %></div>
        <%= f.radio_button :gender, "男" %>男性
        <%= f.radio_button :gender, "女" %>女性
      </div>

      <div class="field">
        <div class="item"><%= f.label "Eメール" %></div>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"text" %>
      </div>

      <div class="field">
        <div class="item"><%= f.label "パスワード" %></div>
        <% if @minimum_password_length %>
        <em>(<%= @minimum_password_length %> 文字以上)</em>
        <% end %><br />
        <%= f.password_field :password, autocomplete: "new-password", class:"text" %>
      </div>

      <div class="field">
        <div class="item"><%= f.label "パスワード（確認）" %></div>
        <%= f.password_field :password_confirmation, autocomplete: "new-password", class:"text" %>
      </div>

      <div class="actions">
        <%= f.submit "新規登録", class: 'btn' %>
      </div>
    <% end %>

    <%= render "devise/shared/links" %>
  </div>
</div>
